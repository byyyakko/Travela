services:
  - type: web
    name: travela-backend
    runtime: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    plan: free
    envVars:
      - key: ALLOWED_ORIGINS
        value: "https://travela.asherethankoh2103.workers.dev,capacitor://localhost,https://localhost,http://localhost:8080,http://localhost:5173"
